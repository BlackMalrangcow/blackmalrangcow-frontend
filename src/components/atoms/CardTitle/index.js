import glamorous from 'glamorous';
import { CardTitle as BaseCardTitle } from 'reactstrap';

const CardTitle = glamorous(BaseCardTitle)({
  width: '427px',
  height: '53px',
});

export default CardTitle;
